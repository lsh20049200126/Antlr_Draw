color is (0,0,255);
origin is(580, 240);									-- 再右移
scale is (80, 80);										-- 恢复原比例
rot is 0;
size is 50;											-- 不旋转
for t from 0 to 2*pi step pi/50 draw(cos(t),sin(t));	-- 画T的轨迹
color is (255,255,0);
size is 18;

for t from 0 to pi*20 step pi/50 draw					-- 画T的轨迹
   ((1-1/(10/7))*cos(T)+(1/(10/7))*cos(-T*((10/7)-1)),
	(1-1/(10/7))*sin(T)+(1/(10/7))*sin(-1*T*((10/7)-1)));


origin is (380,240);

scale is (80,80/3);

rot is pi/2+0*pi/3;

for T from 0 to 2*pi step pi/50 draw(cos(T),sin(T)); --测试

color is (255,0,0);

size is 6;   //size

rot is pi/2+2*pi/3;      --测试

for t from 0 to 2*pi step pi/50 draw(cos(T),sin(T)); --测试场

color is (0,255,255);

size is 12;

rot is pi/2+4*pi/3;      --测试

for t from 0 to 2*pi step pi/50 draw(cos(T),sin(T)); --测试



origin is (20,200);

scale is (40,40);

rot is 0;

color is (255,255,0);

size is 18;

for t from 0 to 2*pi+pi/50 step pi/50 draw(t,sin(t));

origin is (20,240);

color is (255,65,0);

size is 50;

for t from 0 to 2*pi+pi/50 step pi/50 draw(t,sin(t));


origin is (20,280);

color is (0,0,0);

size is 100;

for t from 0 to 2*pi+pi/50 step pi/50 draw(t,sin(t));

rot is 1/0;

for t from 0 to 2*pi+pi/50 step -1*(pi/50) draw(t,sin(t));

for t from -10 to 0 step 1 draw(t,t);